# ---- E3 InsightDeck backend environment ----
# Copy to backend/.env and adjust. Never commit the real .env.

PORT=4004
PUBLIC_BASE_URL=http://localhost:4004

# PostgreSQL (Prisma reads DATABASE_URL). Port 5436 via the dev docker-compose.yml.
DATABASE_URL=postgresql://insightdeck:insightdeck@localhost:5436/insightdeck?schema=public

# Redis — deck cache only (fail-open; a Redis outage never breaks generation). Port 6383.
REDIS_URL=redis://localhost:6383

CORS_ORIGIN=http://localhost:5177

# Narration backend. "llm" is a stub (no keys in this air-gapped build).
NARRATOR_KIND=template

# Insight-detector tuning
CORRELATION_THRESHOLD=0.5
OUTLIER_IQR_FACTOR=1.5
DOMINANT_CATEGORY_THRESHOLD=0.4
MISSINGNESS_THRESHOLD=0.2
MAX_INSIGHTS=12

DECK_CACHE_TTL_SECONDS=3600
